class Point {
  var x: i32;

  init(x: i32) -> Point {
    self.x = x;
    return self;
  }

  fn increment() {
    self.x = self.x + 1;
  }
}

fn main() {
  let p1 = Point(x = 10);
  p1.increment();
  print("Done");
}