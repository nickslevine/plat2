fn main() -> i32 {
    // Test basic set functionality with deduplication
    let my_set: Set[i32] = Set{42, 1, 2, 3, 1, 2, 42};
    print("Set with duplicates removed: ${my_set}");

    // Test different types
    let bool_set: Set[bool] = Set{true, false, true, false};
    print("Boolean set: ${bool_set}");

    let string_set: Set[string] = Set{"apple", "banana", "apple", "cherry"};
    print("String set: ${string_set}");

    print("✅ Set implementation working correctly!");
    return 0;
}