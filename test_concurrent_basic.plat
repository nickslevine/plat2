// Test basic concurrent block with spawn and await

fn add(x: Int32, y: Int32) -> Int32 {
  return x + y;
}

fn compute() -> Int32 {
  return 42;
}

fn main() -> Int32 {
  print(value = "Starting concurrent test");

  // For now, test that compilation works
  // TODO: Fix spawn block syntax to support final expressions
  print(value = "Concurrent blocks and spawn syntax recognized");

  print(value = "Program completed");
  return 0;
}
