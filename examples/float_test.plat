fn main() -> Int32 {
  // Basic float literals
  let pi: Float64 = 3.14159;
  let e: Float64 = 2.71828;

  print("Pi: ${pi}");
  print("E: ${e}");

  // Float arithmetic
  let radius: Float64 = 5.0;
  let area: Float64 = pi * radius * radius;
  print("Circle area with radius ${radius}: ${area}");

  // Float comparisons
  let x: Float64 = 10.5;
  let y: Float64 = 20.3;

  if (x < y) {
    print("${x} is less than ${y}");
  }

  if (x > 5.0) {
    print("${x} is greater than 5.0");
  }

  // Mixed operations
  let sum: Float64 = 100.5 + 50.25;
  let difference: Float64 = 100.5 - 50.25;
  let product: Float64 = 10.5 * 2.0;
  let quotient: Float64 = 100.0 / 4.0;

  print("Sum: ${sum}");
  print("Difference: ${difference}");
  print("Product: ${product}");
  print("Quotient: ${quotient}");

  return 0;
}
