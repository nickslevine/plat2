fn add(x: Int32, y: Int32) -> Int32 {
  return x + y;
}

fn multiply(x: Int32, y: Int32) -> Int32 {
  return x * y;
}

fn main() {
  let a = 6;
  let b = 6;
  let sum = add(a, b);
  let product = multiply(sum, 2);
  print("The result of product is ${product}");
}
