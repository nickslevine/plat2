fn add(a: Int32, b: Int32) -> Int32 {
  return a + b;
}

fn main() -> Int32 {
  print(value = "Testing basic features...");

  // Test function calls and arithmetic
  let result: Int32 = add(a = 5, b = 3);

  // Test multiple prints
  print(value = "Addition works!");
  print(value = "All tests passed!");

  return 0;
}