fn square(x: I32) -> I32 {
  return x * x;
}

fn main() -> I32 {
  let base = 5;
  let multiplier = 3;
  let offset = 10;

  // Complex expression interpolation
  print("Base: ${base}, Squared: ${square(base)}");
  print("Complex: ${(base + multiplier) * 2 - offset}");
  print("Nested: Result is ${base * multiplier + square(2)}");

  return 0;
}