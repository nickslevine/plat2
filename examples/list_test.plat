fn main() -> I32 {
  let arr: List[I32] = [1, 2, 3];
  let len = arr.len();
  let first = arr[0];
  print("Array has ${len} elements, first is ${first}");
  print("About to print array");
  print("${arr}");
  return 0;
}
