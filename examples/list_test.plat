fn main() -> Int32 {
  let arr: List[Int32] = [1, 2, 3];
  let len: Int32 = arr.len();
  let first: Int32 = arr[0];
  print(value = "Array has ${len} elements, first is ${first}");
  print(value = "About to print array");
  print(value = "${arr}");
  return 0;
}
