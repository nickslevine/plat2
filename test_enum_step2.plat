enum Result {
    Success,
    Error(i32)
}

enum Message {
    Quit,
    Move(i32)
}

fn main() {
    let success = Result::Success;
    let quit_msg = Message::Quit;
    print("Multiple enums work");
}