fn main() {
    // Test Option::Some with pattern matching
    let some_value = Option::Some(42);
    let result = match some_value {
        Option::Some(x) -> x + 10,
        Option::None -> 0
    };
    print("Some(42) + 10 = ${result}");
}