use std::json;

fn main() -> Int32 {
  print(value = "Testing std::json compilation...");

  // Just test that it compiles for now
  // We can add real JSON parsing tests once the module is complete

  return 0;
}
