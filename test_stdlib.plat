use std::test;

fn main() -> Int32 {
  let greeting: String = std::test::hello();
  print(value = greeting);

  let sum: Int32 = std::test::add(x = 5, y = 10);
  print(value = "5 + 10 = ${sum}");

  return 0;
}
