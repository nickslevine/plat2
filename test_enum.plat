enum Message {
    Quit,
    Move(i32, i32)
}

fn main() {
    let msg1 = Message::Quit;
    let msg2 = Message::Move(10, 20);

    let result = match msg2 {
        Message::Quit -> 0,
        Message::Move(x, y) -> x + y
    };

    print("Result: ${result}");
}