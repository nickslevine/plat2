newtype DocumentID = I32;
newtype UserID = I32;

fn get_document(id: DocumentID) -> Int32 {
  return 100;
}

fn get_user(id: UserID) -> Int32 {
  return 200;
}

fn main() -> Int32 {
  let doc_id: DocumentID = 1;
  let user_id: UserID = 2;

  let result1 = get_document(doc_id);
  let result2 = get_user(user_id);

  return result1 + result2;
}
