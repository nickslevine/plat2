// Test that different newtypes are not interchangeable
newtype DocumentID = I32;
newtype UserID = I32;

fn get_document(id: DocumentID) -> I32 {
  return 100;
}

fn main() -> I32 {
  let user_id: UserID = 2;

  // This should fail - UserID and DocumentID are distinct types
  let result = get_document(user_id);

  return result;
}
