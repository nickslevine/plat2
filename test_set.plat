fn main() -> i32 {
    let my_dict: Dict[string, i32] = {"key1": 42};
    print("Initial Dict: ${my_dict}");

    let success = my_dict.set("key2", 100);
    print("Set result: ${success}");

    let length = my_dict.length();
    print("New length: ${length}");

    return 0;
}