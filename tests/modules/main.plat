use math;
use utils;

fn main() -> Int32 {
  let x: Int32 = math::add(a = 10, b = 20);
  print(value = "10 + 20 = ${x}");

  let y: Int32 = utils::double(x = 15);
  print(value = "double(15) = ${y}");

  let z: Int32 = utils::square(x = 5);
  print(value = "square(5) = ${z}");

  return 0;
}
